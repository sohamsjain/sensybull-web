# Sensybull Web Frontend

## Tech Stack
- Next.js 15 (App Router), TypeScript, Tailwind CSS v4, shadcn/ui
- Socket.IO client for real-time filing events
- JWT auth: a short-lived access token in localStorage, the refresh token in an httpOnly cookie (CSRF double-submit: the token comes back in the sign-in response body and is stored as `sensybull:csrf` — the matching `csrf_refresh_token` cookie belongs to the API's host, which this origin cannot read, and is only a fallback). `src/lib/api-client.ts` owns the session — it renews the access token proactively before `exp`, single-flights concurrent refreshes, and ends a session only on a 401/403/422 from `/auth/refresh` (never on a network error, 429 or 5xx). Sign-in longevity is therefore capped by the backend's `JWT_REFRESH_DAYS` and the refresh cookie's own max-age, not by anything in the client

## API
- Base URL: `NEXT_PUBLIC_API_URL` (default: `https://api.sensybull.com/api/v1`)
- Auth: `POST /auth/login`, `/auth/register`, `/auth/google`, `/auth/refresh`, `GET /auth/me`
- Events: `GET /events/` (auth, watchlist-filtered — backs the feed's "My companies" scope, ordered by `filing_date`), `/events/all` (public, ordered by receipt), `/events/types` (small canonical category list — backs the feed's event-type filter), `/events/company/:id` (auth, per-company history). **Feed filters run in the API** (Sept 2026): all three list routes take `important`, `event_type`, `sector`, `cap`, `source`, `sentiment`, `moved`, `since`, `q` (multi-values comma-separated, unknown values 400) — build the query with `filtersToQuery()` from `src/lib/feed-filters.ts`, never by hand. `GET /events/facets` gives per-option counts for the filter panel (each counted with every other filter applied), and `/feed/views` stores a signed-in reader's saved views. Live socket events arrive unfiltered, so `useEvents` runs them through `matchesFeedFilters()` — the same rules, mirrored from the API's `app/services/feed_filters.py`; keep the sector list, cap-bucket bounds and predicates in sync (`src/lib/__tests__/feed-filters.test.ts` pins the bounds). Every event carries a boolean `important` (backs the feed's All/Important toggle — use `isImportant()` from `src/lib/event-actions.ts`, which handles legacy payloads) and `event_types` (use `matchesEventType()` from `src/hooks/use-events.ts`). Only the 8-K family is ingested among SEC forms since the July 2026 rollback; older events keep other `signal_type` values. Press releases arrive as `signal_type="PR"` (`source` = wire name, `edgar_url` = article URL; use `filedPhrase()` from `src/lib/forms.ts` for "filed/issued" copy). A PR event gains `filing_url` once its SEC filing arrives — delivered via socket event `filing_event_update` (same payload shape; replace by `id` in state). A briefing may carry `evidence`: up to 3 verbatim quotes from the filing, each with a deep link that opens the source document on sec.gov highlighted at that passage (a `#:~:text=` fragment built and verified at ingest). Read it through `evidenceEntries()` in `src/lib/evidence.ts` — it is absent on facts-only briefings, press releases, and everything stored before it shipped
- Watchlists: CRUD at `/watchlists/`, company management at `/watchlists/:id/companies`
- Companies: `GET /companies/?q=...` (search by ticker or name), `GET /companies/search?q=` (typeahead), `GET /companies/:id/bars` (chart OHLCV; optional `end` pages backwards through history, `lookback` also accepts `2Y`/`5Y`), `GET /companies/:id/quote` (last price + day change; `change_pct` is null when there's no previous close, `stale: true` when it's the daily-synced price rather than a live one), `GET /companies/quotes?ids=a,b,c` (the same payload for up to 120 companies at once — backs the price on every feed and watchlist row; companies with no price are absent from the map rather than an error)
- Alerts: `GET/PUT /alerts/preferences`, `GET /alerts/notifications`, `GET /alerts/channels`, Web Push at `/alerts/push/*` (see `src/lib/push.ts` + `public/sw.js`). The sensitivity UI is binary (Important only ↔ Everything) mapped onto the API's `max_tier` (1 ↔ 3)
- Events (single): `GET /events/all/:id` (public, permalinks)
- Watchlist inbox: `GET /watchlist/` (companies + unread counts under `items`), `POST /watchlist/:companyId/read`, `PUT /watchlist/:companyId/mute`. Bulk counterparts back multi-select — `POST /watchlist/read`, `PUT /watchlist/mute`, `POST /watchlist/remove`, all taking `{company_ids}` — and drop ids the user no longer follows instead of failing the batch (trust the echoed `company_ids`)
- Fundamentals (screener.in-style company pages, Sept 2026 — plan in sensybull-api `docs/FUNDAMENTALS_PLAN.md`): `GET /fundamentals/:symbol` (public; one column-oriented payload: header ratios, growth grids, pros/cons, then `quarterly.{income,balance,cashflow,ratios}` and `annual.{…}` — all four statements at both granularities, oldest → newest, amounts in whole dollars, `status` of `ready | building (202) | unavailable | empty`) and `GET /fundamentals/:symbol/documents` (EDGAR 10-K/10-Q/proxy links + our last 10 briefings). Types in `src/types/fundamentals.ts`; fetch helpers in `src/lib/fundamentals/api.ts` (server-side, ISR; a non-ready answer is re-fetched uncached so a cached "building" can't pin the page). Row order, labels and formats live in `src/lib/fundamentals/rows.ts`, mirroring the API's `rows.py` — keys must match (`src/lib/__tests__/fundamentals-rows.test.ts` pins the list). Balance sheet reads Assets → Liabilities → Equity, the 10-K order. `GET /companies/search` is public now and returns `has_fundamentals`; route a result to `/company/<ticker>` when true (`companyHref()`)
- Share links: `GET /share/:symbol` (public share info, no internal IDs), `POST /watchlists/track` (auth, idempotent add-by-ticker), `POST /share/events` (public funnel analytics). See `docs/TRACK_LINKS.md`
- WebSocket: Socket.IO namespace `/feed`, auth via `{token}` dict, events: `filing_event`, `filing_event_update`, `connected`, `price_reaction`. The `/feed` socket is owned once at the dashboard layout by `SocketProvider` (`useSocket`) and shared by all pages — it persists across client-side navigation
- Thesis monitoring is the product direction as of Sept 2026 (see `PRODUCT_VISION.md`) — the landing page sells it, the API does not implement it yet. The thesis surfaces are being rebuilt: thesis capture per company, an `intact | watch | drift` verdict per event, a `THESIS DRIFT` alert. Read `PRODUCT_VISION.md`'s "Scope discipline" before adding to it; the July 2026 rollback happened because the first attempt grew without a stop condition
- Still removed, and not part of the thesis rebuild: `GET /events/catalysts` (key dates live inside updates and the company sheet) and `GET /movers`. The old `/positions/*` endpoints and the `thesis_alert` socket event are gone too — the rebuild designs its own contract rather than restoring theirs

## Project Structure
- `src/types/` — API and event type definitions
- `src/lib/` — API client (auto-refresh on 401), Socket.IO wrapper, utilities (`utils.ts` also configures `cn()`); `chart-theme.ts` (hex mirror of the design tokens for the canvas chart); `chart-signals.ts` (which filings earn a mark on the price chart and where the arrow goes — weekly ATR, session pinning, marker grouping); `event-actions.ts` (isImportant, AI-chat copy prompt, share/permalink helpers); `feed-filters.ts` (the feed's one filter shape — URL keys, API query, saved-view dict, the live-event predicate, cap buckets, presets; mirrors the API's `app/services/feed_filters.py`); `deal-terms.ts` (label/value/order rules for `briefing.deal_terms` — Title Case values, snake_case keys; mirrors the API's `app/utils/deal_terms.py`, keep in sync. Both the Deal Terms panel and the AI-chat prompt read terms through `dealTermEntries()`); `company-name.ts` (`displayCompanyName()` — EDGAR stores filer names shouted ("PURE CYCLE CORP") while newswire names arrive already cased, so every surface that prints a company name titles the shouted ones and passes the rest through untouched)
- `src/hooks/` — useAuth, useSocket, useEvents (REST+Socket merge; server-side filters, the last answer stays up while a new filter loads, errors are distinct from "no matches"), useFeedFacets (panel counts, fetched only while the panel is open), useFeedViews (saved views), useWatchlists, useWatchlistInbox (inbox + live unread + bulk actions), useWatchlistSelection (multi-select mode; range logic in `src/lib/selection.ts`), useCompanyEvents (per-company history), usePaneWidth (resizable pane), useBars (chart data + `loadOlder()` history paging), useQuote (one company's price for the pane header), useQuotes (batch prices for a list of company ids — both poll 60s and pause on hidden tabs)
- `src/context/` — AuthProvider (login/register/google/logout), SocketProvider (session-wide `/feed` socket + `useSocket`)
- `src/components/ui/` — design-system primitives on top of shadcn/base-ui: `button`, `input`, `search-input`, `chip` (Chip/ChipRow/SegmentedControl), `icon-button`, `badge` (Badge/CountBadge/ImportantMarker/StatusDot/MetaLabel), `section` (Section/GroupLabel/Card), `data-table`, `empty-state`, `skeleton`, `switch`, `tooltip`, `kbd`, `icons` (the single icon set), plus dialog/sheet/dropdown-menu/app-toaster
- `src/components/feed/` — FilingCard (flat row, no box: ticker · name · market cap · sector · price · time, then category, then headline; in the feed the sector and category are buttons that pivot the filters to them), FilingList (divider-separated; earlier pages load on scroll via an IntersectionObserver sentinel, with a visible button as the fallback), FeedToolbar (row one: scope + search + the Filters button + live indicator; row two: All/Important, then one removable pill per active filter — or, with none on, one-click presets that read back as ordinary pills — then the match count and the Views menu), FilterPanel (the right-hand sheet on `F`: priority, time, price reaction, what happened, sector, market cap, source, tone — each option with its facet count; changes apply live, zero-count options dim but stay clickable), FeedViewsMenu (saved views: the trigger names the view on screen, and offers "Update <view>" once the filters drift from it), UpdateActions (Read the filing / Copy for AI chat / Share — shown only on expanded updates), CatalystsTable ("Key dates"), DealTerms, EvidenceList ("From the filing" — quoted source text, each quote a link to the highlighted passage on EDGAR), PriceReactionStrip, CompanyLogo
- `src/components/watchlist/` — FirstRun (what a signed-in reader with no companies sees: inline search + suggested tickers, instead of "pick a company" addressed to someone with none), WatchlistPanel (chat-list chrome: title + one ⋮ menu, search, then All / Unread chips under it; also fetches the list's prices in one batch), WatchlistItem (three bands: name + time, price + day change + unread, then a two-line headline — **no ticker anywhere on the watchlist page**; the name links to the company's financials in a new tab, the row opens its history here), Conversation (company filing history; the header is avatar + name + live price, nothing else), FilingMessage (a flat research entry, not a chat bubble), CompanyAvatar (the one company mark — `feed/company-logo.tsx` wraps it; `fallback="initials"` on the watchlist so a missing logo never shows a ticker)
- `src/components/fundamentals/` — the company page's parts: StatementSection (one card per statement: title, unit line and the Quarterly/Annual switch, wrapping FinancialTable; below `sm` it drops the card's border and padding and the section breaks the page gutter, because at 390px a border and its inset cost a column), FinancialTable (sticky row labels, every period rendered and **pinned to the newest column** by a ResizeObserver, fiscal-year banding via `fiscalYearStarts()`, striped rows counted over *rendered* rows so an expanded breakdown doesn't break the alternation, expandable breakdown rows with the +/− on the right so labels stay flush, quality-flag footnotes; it uses `border-separate` so each cell draws its own rule, with the first row skipping its top rule so it doesn't double the header's. The frozen column has no edge or shadow, and the part-column it cuts in half is left cropped — covering it costs a whole column of width, and a reader scrolling a table already knows the row continues. A subtotal is marked by a **2px `border-line-strong` rule** above it, not a hairline: the rule is what builds the habit of knowing that the figure under the first one is Total Assets, so it has to be legible at a glance. The +/− opens beside its own row label, not aligned down the column, and is accent-coloured so it reads as the control it is. The row stripe is an **opaque** tint on the `tr` (`bg-stripe`) because the frozen label cell inherits the row's background and a translucent one would let figures scroll through underneath it; the column band is a **translucent** tint on the `td` (`bg-band`), which is what makes a banded cell on a striped row darker than either alone. Don't swap those), RatioGrid (the header stats as screener lays them out — label left, figure right, three columns; the unit is a separate dimmed span, so all 21 computed ratios are printed rather than nine; only the price row goes live via `useQuote`), AboutSection (description + rule-generated pros/cons, never edited client-side), GrowthGrids, SectionNav (the sub-bar, sticky directly under the app's own bar; the company's name is its first item and jumps back to the top. Jumps are instant — a smooth scroll across a page this tall is a second of watching rows fly past. The current section is **measured**, not observed: an IntersectionObserver needs a static `rootMargin`, and this bar's distance from the top of the viewport is not static — it sits below the app bar, so the band starts at 93px, not at the bar's own 44. Selected is an underline plus accent text, not a filled pill: it marks a position in the page, not a filter), DocumentsSection, CompanyActions (Track / Updates / Share), BuildingPoll (polls a 202 and `router.refresh()`es), CompanySearch (the `/company` home). Formatting rules in `src/lib/fundamentals/format.ts` — **every statement amount is in $ Mn as a whole number**, stated once under the header; there is no per-table unit toggle and no mixed precision inside a column. Statement tables and the header stats are set at `text-body`, not `text-meta`: the page should read at the "Extra large" step while the reader is still on the default one. Header stats come back as a `{value, unit}` pair so the unit can be dimmed. **Every link into `/company/<ticker>` opens a new tab** — company names on watchlist rows, the conversation header, feed rows, search results and the palette all go through `src/lib/fundamentals/links.ts` (`fundamentalsHref`, `NEW_TAB`, `companyHref`, `companyLinkProps`)
- `src/components/company/` — PriceChart (+ ChartLegend, ChartMarkerTooltip; always the last 3 months, no period switcher — the full-pane chart pans and loads older history itself), StockQuote (price + day change in the conversation header; formatters in `src/lib/quote.ts`). The CompanySheet slide-over is gone (Sept 2026): a company name now opens the financials page in a new tab instead
- `src/components/layout/` — TopNav (the one horizontal bar on every width: wordmark, **Watchlist** and **Feed** from `nav-items.ts`, NavSearch, then ProfileMenu; guests get a theme toggle and Sign in instead of the menu), NavSearch (company typeahead in the bar — a result opens its financials in a new tab; below `sm` it collapses to a glyph that opens the ⌘K palette), ProfileMenu (every preference folded in: theme, text size, alert preferences, keyboard shortcuts, log out). There is no rail, no bottom tab bar, and no Financials destination — the search box is the way to a company page
- `src/components/auth/` — Login/register/forgot-password forms
- `src/app/(auth)/` — Auth pages (centered layout, no sidebar)
- `src/app/(dashboard)/` — Dashboard pages (top navbar + main)
  - `/watchlist` — default landing for signed-in users; two-pane UI (resizable/collapsible company list + filing history), chart toggle swaps the right pane for a full-size price chart (`/chats` redirects here). The list is sorted by last activity only, like a chat list — unread never reorders it. Multi-select works like WhatsApp Web: the ⋮ menu offers "Select companies", the header becomes "N selected" with ✕ and its own ⋮ menu (select all, mark as read, mute/unmute, remove — remove confirms inline). It lives in WatchlistPanel because ranges and "select all" follow the panel's own filters, and the page only learns whether the mode is on so its ↑/↓/Esc shortcuts stand down
  - `/feed` — the live stream, in two scopes: **My companies** (`GET /events/`, signed-in default) and **Everything** (`GET /events/all`, the public stream, all a guest ever sees). Plus the All/Important toggle, event-type chips, and search (ticker, company name, or headline), no sidebar. Substack/Twitter-style flat list: rows separated by simple dividers, no card boxes. The two scopes come from different endpoints in different orders, so live socket events are slotted in with `orderKeyFor(scope)` and, in "My companies", filtered to the companies the reader follows. Prices need a session, so signed-out readers see rows without them
  - `/company` — search-first home for company financials (public); `/company/[symbol]` — the screener-style page, a **server component** rendered from one API payload (tables are in the HTML; only price, expand/collapse, the Quarterly/Annual switches, section nav and the building poll hydrate). Signed-out readers see the same page. Reached from the navbar search, always in a new tab. The API also resolves former tickers, other share classes and BRK.B/BRK-B spellings; when the payload's `company.ticker` differs from the URL, the page redirects (307, not 308 — tickers get reused) so each company has one URL
  - `/e/[id]` — public per-event permalink (backed by GET /events/all/:id), rendered expanded
- `src/app/add/[symbol]/` — public "Track on Sensybull" deep link (`/add/MU`): SEO/OG page + client flow that adds the ticker to the watchlist, preserving intent through auth via `src/lib/pending-action.ts` (see `docs/TRACK_LINKS.md`)
- `src/app/embed/[symbol]/` — iframe-able track button (route handler, frameable by design; header carve-out in next.config.ts)
- `src/components/share/` — TrackButton, ShareDialog (company page actions), AddFlow
- `src/components/shortcuts-sheet.tsx` — the `?` overlay listing every shortcut the app listens for. It is the documentation for them: add or remove a key handler in `filing-list.tsx` or `watchlist/page.tsx` and this list changes in the same commit
- `src/lib/share.ts` (canonical link/snippet builders) + `src/lib/share-analytics.ts` (funnel events); global toast in `src/components/ui/app-toaster.tsx` (mounted in root layout)
- Removed pages: `/calendar` (catalyst calendar; key dates live inside updates and the company sheet) and `/movers` (removed with the `GET /movers` API endpoint), both July 2026. The old `/positions` page went with the same rollback; thesis surfaces are being rebuilt from scratch under the Sept 2026 direction, not restored
- `src/components/landing/landing-page.tsx` is the marketing page, and it is deliberately ahead of the product: it describes thesis monitoring the API doesn't ship yet. The illustrative portfolio counts and timeline are labelled "Illustrative." — keep that label. Never let a mock on this page imply a verified figure

## Product rules (see PRODUCT_VISION.md)
- Updates collapse to headline-only; summary, key dates, and action buttons appear only when expanded. No per-update "investor takeaway" bullets, no significance explainers
- Priority is binary: an update is Important or it isn't. Never reintroduce HIGH/MED/LOW in the UI
- Every expanded update gets UpdateActions (source link, copy-for-AI-chat, share) — keep feed cards and watchlist messages identical here
- The price chart marks the updates that *moved* the stock, not every filing: a session qualifies when its move clears `ATR_MULTIPLE` of the weekly ATR (or the backend already flagged the event `explosive`). The arrow sits on the side the stock left — below the candle when it rose, above when it fell — with a price-space gap so it never touches the wick, and hovering it reads the headline behind the move. "All" is a toggle, never the default. Thresholds live in `src/lib/chart-signals.ts`; don't scatter them into the component
- The company page is one wide reading surface, not a reading column: a statement is a dozen-plus periods across. Prose (About, Analysis, Documents) is capped back to a readable measure inside that width
- A statement is one section with a Quarterly/Annual switch, never a Quarters section and a P&L section. Amounts are always $ Mn whole numbers. Rows stripe, and on a **quarterly** table the first quarter of each fiscal year bands — banding marks where one year ends and the next begins, so it is meaningful rather than decorative, and an annual table gets none (every column there is already a year). The two crossing into a grid is intended. Tables carry no hover highlight: the reading aids are structural, not pointer-following. A part-column cut off by the frozen labels is left cropped rather than covered
- An expanded update shows its work: the quotes in EvidenceList are the filing's own words, rendered as quotes and never edited, paraphrased or truncated in the client. Order inside an expanded update is summary → evidence → deal terms → key dates → price reactions → actions

## Related Projects
- Backend API: ~/Projects/sensybull-api (Flask)
- API changes log: see API_CHANGES.md in this repo
- When unsure about an endpoint's shape, read the backend route file directly

## Discovery (crawlers and agents)
- `src/app/page.tsx` is a **server component** so the landing page is in the HTML — most AI crawlers never run JavaScript. Signed-in readers are forwarded by `SignedInRedirect`; an inline script marks `<html data-session>` before paint when localStorage holds a session (keys mirror `hasSession()`), and `globals.css` hides `[data-landing]` until auth decides, so the landing never flashes for them. Don't turn the page back into a client component
- Canonicals are per page. The root layout deliberately has none (a canonical there is inherited and declared every page a duplicate of `/`). `metadataBase` uses `SITE_URL` — the bare domain; www 301s to it
- `/e/[id]` is server-rendered (`src/lib/events/api.ts` → `EventPermalink`) with NewsArticle JSON-LD. Only events with evidence are indexable; the rest are `noindex, follow`, and the sitemap lists only evidence-bearing events
- `/robots.txt` is a route handler over `src/lib/robots.ts` (policy: search engines and AI search/answer agents welcome, AI training crawlers refused, `Content-Signal: search=yes, ai-input=yes, ai-train=no`). New signed-in or auth routes go in its `PRIVATE_PATHS`
- `/sitemap.xml` (`src/app/sitemap.ts`) = static pages + the API's `GET /discovery/sitemap`; ISR every 6h, static pages only if the API is down
- `/.well-known/api-catalog` (RFC 9727) and the homepage's RFC 8288 `Link` header both come from `src/lib/api-catalog.ts`
- Not published, on purpose: OAuth/OIDC discovery, auth.md, MCP/A2A cards, WebMCP, ARD, DNS-AID. Each describes something we don't run (auth is first-party JWT; there is no MCP server or agent). Publish one only alongside the real thing

## Conventions
- Files: kebab-case. Exports: PascalCase for components, camelCase for hooks/utils
- All dashboard/auth components are client components (`"use client"`)
- Feed state (scope + every filter) lives in `(dashboard)/layout.tsx` via React context as one `FeedFilters` object (URL params `s` for scope, then `f t sec cap src snt m d q` — `filtersToParams()`/`filtersFromParams()`; unknown URL values are dropped, not sent). Scope is `null` until auth resolves — nothing fetches before it settles — then falls back to the reader's remembered choice (`feed-scope` in localStorage), defaulting signed-in readers to their own companies and guests to everything
- **Design system: see `docs/DESIGN_SYSTEM.md` — read it before touching UI.** Every colour, font size and radius comes from the tokens in `src/app/globals.css` (`bg-canvas`, `bg-surface-hover`, `border-line-subtle`, `text-ink-muted`, `text-label`, `rounded-md`, …). Never write a palette class (`slate-*`, `indigo-*`) or a raw hex in a component; the only exceptions are the canvas/OG/embed surfaces noted in that doc. Icons come from `src/components/ui/icons.tsx` — never from `lucide-react` directly, never hand-rolled SVG
- Dual theme (class-based dark mode). Accent: indigo, reserved for interactive elements; selected states are solid accent with `text-brand-on` — selection contrast must be obvious at a glance. Decorative color is avoided: category/form tags are plain muted text (`MetaLabel`), `danger` is reserved for the Important marker and negative price data, `success` appears only on positive price data
- New `--text-*` / `--color-*` tokens must also be added to the lists in `src/lib/utils.ts`, or `cn()` silently drops the size when a colour class follows it (`src/lib/__tests__/cn.test.ts` covers this)
- Type floor is 12px (`text-micro`) — there is no 10px tier. Radius steps are 4/6/8/12px, each ≥1.33× the last; don't squeeze a new value between two existing ones. Nothing interactive is under 28px, and the default control height is 36px
- Errors are not empty states: `api()` throws `ApiError` with a `status` (`0`/`isOffline` = never reached the server), so "couldn't connect" and "not found" stay distinct. Report a failed action with `toast({ tone: "danger" })` — an empty `catch {}` around a mutation is a bug
- The UI presents a single watchlist (adds go to the user's first list via `src/lib/default-watchlist.ts`); the API still supports multiple lists
- Brand assets: `public/logo.png` is the bull mark on a transparent background, black ink — surfaces flip it to white with Tailwind's `invert`/`dark:invert`, which leaves the alpha alone. `public/logo-tile.png` is the opaque counterpart (dark tile, white bull) for surfaces that paint their own background: the OG/Twitter card image and the Web Push notification icon in `public/sw.js`. `src/app/favicon.ico` (16→256 frames) and `src/app/apple-icon.png` are the same tile, wired up by Next's file conventions — the mark is ~1.9:1, so it reads as a soft silhouette in a 16px tab. Never give the mark a baked-in background
- Unit tests: `npm test` (vitest, `src/**/*.test.ts`)
